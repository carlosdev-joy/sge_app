fastapi==0.115.5
uvicorn[standard]==0.32.1
httpx==0.27.2
pyodbc==5.2.0
python-dotenv==1.0.1
paramiko==3.5.0
# Fernet das senhas de dbo.etl_conexao (services/conn_crypto.py) — já vinha
# como dependência do paramiko; pin explícito porque agora é uso direto.
cryptography==49.0.0
# SDK oficial da Anthropic — assistentes IA do Caixa Seguro
# (services/caixa_ia.py, provedor 'anthropic'; import tardio, só exigido
# quando esse provedor está configurado).
anthropic==0.116.0
