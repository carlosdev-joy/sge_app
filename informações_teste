# Ver qual imagem está rodando (confirma que é a antiga)
docker compose ps
docker compose images orquestra-api

# Rebuild + restart apenas da API (sem derrubar o resto)
docker compose build orquestra-api && docker compose up -d --no-deps orquestra-api

# Acompanhar os logs até aparecer "Application startup complete."
docker compose logs -f orquestra-api
docker compose restart ui-nginx

docker compose logs orquestra-api --tail=20
curl -s http://localhost/orquestra/health

cp /opt/git/deploy.sh /opt/git/deploy.sh.bak   # backup do antigo
curl -fsSL https://raw.githubusercontent.com/carlosdev-joy/sge_app/main/scripts/deploy.sh \
  -o /opt/git/deploy.sh
# OU copie manualmente o conteúdo de scripts/deploy.sh

curl -s http://localhost/orquestra/health
# deve retornar {"status":"ok"}

curl -I http://localhost/js/app.js
# deve retornar Content-Type: application/javascript (não text/html)
