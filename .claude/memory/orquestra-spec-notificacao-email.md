---
name: orquestra-spec-notificacao-email
description: "Spec do nó de e-mail do Orquestra (Admin › E-mail + nó `email` no pipeline, envio por sendmail via SSH no servidor do DataStage); aprovada 2026-09-10, F1 em PR"
metadata: 
  node_type: memory
  type: project
  originSessionId: 55c7dc0b-089c-4e39-9b3c-c60297f1190c
  modified: 2026-09-11T06:46:35.992Z
---

**Spec:** `docs/spec-notificacao-email.md` no repo Orquestra. **Status: APROVADA 2026-09-10 ("aprovado", decisões assumidas de §8 aceitas). ✅ **F1 MERGEADA 2026-09-11 (PR #388, squash `2e94d1d`)**; ✅ **F2 MERGEADA 2026-09-11 (PR #389, squash `0acc4d5`)**; 🔧 **F3 = PR #390 ABERTA (`feat/email-f3`), aguarda autorização de merge**.** Origem: documento do usuário `2026-09-10-notificacao-email.md` (enviado pelos Utilitários do DEV para `/dados/bi` do container `orquestra-dev-sshd-amostra`), avaliado contra o código.

**Fatos validados em produção (pelo usuário, via DataStage):** relay `smtp.adcorp.intranet` sem autenticação, remetente `orquestra@caixavidaeprevidencia.com.br`, Postfix em `lnxprd021`, `mailx` no PATH, entrega confirmada no domínio interno. O worker do Airflow NÃO foi testado no relay → envio a partir do servidor do DataStage por SSH.

**Correções que a avaliação fez ao documento original:** o nó de notificação Teams JÁ tem tela em Etapas (`pages/Jobs.tsx`) e Fluxos (`PainelNotificacao.tsx`) — só a leitura em runtime falta (o factory embute `grupo_id`/`template_id`/`mensagem` como literais); `etl_app_config` já existe; o nó roda no worker com `SSHHook` (paramiko da API é só do console); a malha tem nós próprios (`malha_nos.py`) — fora da spec; segurança: nada digitado pelo usuário na linha de comando (`sendmail -t -i -f <remetente>` + MIME via stdin), anexo com raízes permitidas.

**Decisão do usuário (2026-09-10):** anexo = diretório entre as raízes parametrizadas + **nome livre** (o arquivo ainda vai ser gerado na corrida): validar existência só na hora; ausente → envia sem anexo com aviso, nunca falha por isso. Placeholders no nome (`relatorio_{odate}.xlsx`).

**Assumidos a confirmar na aprovação (§8):** `sendmail -t -i` + MIME (não `mailx`); nó no padrão do nó de notificação; toda falha de envio falha a task; aba só `acao_admin`; domínios permitidos opcionais.

**F1 — PR #388 MERGEADA `2e94d1d` (2026-09-11):** migration 111 (5 chaves `email_*`, `etl_pipeline.email_destinatarios`, `etl_email_log`), `services/email_mime.py` ↔ espelho `dags/utils/email_envio.py` com teste anti-drift, `services/email_config.py`, `run_sendmail` em `services/ssh_datastage.py`, `routers/email.py` (status/config/testar/log), aba Admin › E-mail (`EmailTab.tsx` + `lib/emailAdmin.ts`) e bancada seção 9. Validação: 5135 pytest + as 8 falhas pré-existentes, `tsc -b` limpo, eslint 195=195 contra o baseline do HEAD, smoke DEV 12/12 com sendmail de mentira.

**Decisão da revisão adversarial da F1:** o comando virou `sendmail -t -i -f <remetente>`. Sem o `-f` o Postfix assume o usuário do SSH: bounce na caixa errada e relay que policia o MAIL FROM descarta **depois** do rc 0 (falso verde). O remetente é o único argumento de configuração na linha de comando — passa por `EMAIL_RE` e `shlex.quote`. Outros achados aplicados: JSON das listas limitado a 1000 (teto de `config_value`, senão o MERGE dá 500 opaco), `_sem_quebra` por `splitlines()`, `cabecalho_extra` sem cabeçalhos estruturais (um `Bcc` extra seria ENTREGUE pelo `-t`), raiz `/` recusada, banco fora do ar deixou de se disfarçar de "migration pendente".

**Why:** e-mail é o canal que a operação pede; o desenho evita gatilho implícito e segredo em tela, e reaproveita o canal já validado.

**How to apply:** F1 entregue; F2 = nó `email` no pipeline (Etapas + Fluxos + factory + operador no worker, herdando a lista do pipeline; exigir `S_ISREG` no anexo em tempo de envio — ressalva da revisão da F1); F3 = notificação Teams em runtime; F4 = docs/smoke (homologação — DEV não tem Postfix nem relay). Relacionado: [[orquestra-spec-maestro-parametros]] (mesmo padrão de leitura em runtime).

**⏳ Deploy em produção da F1 PENDENTE:** migration 111 na etapa 6c, `api/`, `dags/` (arquivo novo em `dags/utils/` → **restart do worker**), `dist/`, `config/` → **n**. Opcional no `.env` da API: `EMAIL_SENDMAIL_BIN` (padrão `/usr/sbin/sendmail`; em `lnxprd021` pode ser `sendmail.postfix`). Depois: Admin › E-mail → remetente, ligar, raízes, *Testar*, e **conferir o `Return-Path`** do e-mail recebido (o envelope-from do `-f`) — é o que separa 'entregue' de 'aceito e descartado depois'.

**F2 — PR #389 MERGEADA `0acc4d5` (2026-09-11):** nó `email` no canvas. Decisão estrutural: a config do nó vive na MESMA coluna do nó Teams (`etl_pipeline_job.notify_json`) — o `job_type` distingue — e o factory emite **só os nomes**, com o `EmailOperator` (`dags/utils/email_operator.py`) lendo tudo do banco em runtime. É isso que cumpre 'mudar destinatário vale na próxima corrida sem republicar' (o nó Teams faz o contrário e por isso exige regerar). Teste-âncora `test_ancora_factory_nao_embute_a_config`. Duas listas somadas (nó + `etl_pipeline.email_destinatarios`). Validação: 5192 pytest + as 8 pré-existentes, eslint 195=195, smoke DEV 18/18 com o operador rodando DENTRO do worker.

**Desvios da spec conscientes (registrados na §8 do doc):** (1) o nó fica **só no canvas**, não na tela Lista — `sql` e `aguarde` também não estão lá, e a lista `JOB_TYPES` daquela tela nunca os ganhou; (2) a segunda lista `VALID_JOB_TYPES` em `dags/etl_pipeline_job_register.py` segue defasada de propósito (exige lineage que nó especial não tem).

**Revisão adversarial da F2 REPROVOU na 1ª rodada — 6 defeitos, todos corrigidos e com teste-âncora:** (1) decisão com nó especial no ramo gerava `log_start_<nome>`, task que nó sem t_start não tem → `BranchPythonOperator` falha e derruba o pipeline (valia também para `sql`/`aguarde`, pré-existente, corrigido junto); (2) `{linhas}` sempre vazio — o upstream real é `log_end_<job>`, mas o `rows_out` está no XCom de `<job>`; (3) `{odate}` usava `ds_nodash` (início do intervalo = dia anterior num fluxo diário) em vez da data de referência da corrida (`etl_pipeline_execucao`) — o anexo do dia nunca era achado; (4) o 503 da migration ausente era engolido por `except Exception: pass` E abortava a gravação dos campos da 017 em TODO save de QUALQUER pipeline; (5) a lista do FLUXO não passava pela allowlist de domínios; (6) `{status}` era fixo 'concluído' — afirmaria sucesso depois de um Aguarde 'todas_terminarem' sobre etapa falha. ⚠️ LIÇÃO: um teste que fabrica o identificador do upstream (`upstream_task_ids = {'carga'}`) esconde justamente o defeito que deveria pegar — usar o que o factory gera de verdade.

**⏳ Deploy em produção de F1+F2 PENDENTE (conjunto):** migration **111** na etapa 6c; `api/`; `dags/` com **RESTART DO WORKER** (`dags/utils/email_envio.py` e `email_operator.py` são novos e `dags/utils/` é cacheado); `dist/`; `config/` → **n**. Opcional no `.env` da API: `EMAIL_SENDMAIL_BIN`. Depois: Admin › E-mail (remetente, ligar, raízes de anexo), *Testar*, **conferir o `Return-Path`** do e-mail recebido; e, para usar o nó, arrastar **E-mail** no fluxo e **republicar uma vez** (dali em diante texto e destinatário mudam sem republicar).

**F3 (2026-09-11, `feat/email-f3`):** o nó de notificação Teams deixou de embutir `grupo_id`/`template_id`/`mensagem` no código gerado — o helper lê `notify_json` no disparo. ⚠️ **Exige republicar UMA VEZ** os pipelines com nó de notificação (as DAGs publicadas são autocontidas e seguem com o texto antigo). O teste-âncora `test_ancora_fonte_sem_delta_e_byte_identico_ao_de_main` (`tests/test_dag_factory_espera.py`) cobra que toda fase que muda o fonte gerado DECLARE seu delta — o helper sai em TODA DAG, então até pipeline sem notificação muda.

**Revisão adversarial da F3 REPROVOU:** eu tinha acrescentado um `raise` quando faltava canal. Ele guardava o caso IMPOSSÍVEL (a API exige `grupo_id`) e deixava passar o REAL (grupo apagado/inativo/sem webhook → card ia para o webhook padrão do Variable, task verde, canal errado); e o `except Exception` genérico fazia um aviso lateral DERRUBAR a corrida (nó apagado/renomeado sem republicar → publish `upstream_failed`, Dataset não publicado, cascata de dependentes parada). ⚠️ REGRA: a task de notificação **nunca** reprova o pipeline e **nunca** posta sem canal resolvido. ⚠️ LIÇÃO: teste que só faz `assert '<trecho>' in src` passa com a trava guardando a condição errada — testar EXECUTANDO o helper.
