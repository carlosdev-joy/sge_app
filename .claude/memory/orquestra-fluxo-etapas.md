---
name: orquestra-fluxo-etapas
description: "Análise completa (2026-07-05) do editor de fluxo/sequências do Orquestra — o que existe, bugs ativos e roadmap em ondas acordado com o usuário"
metadata: 
  node_type: memory
  type: project
  originSessionId: 6be00b90-dd3a-4cf7-bd42-c91cbf215942
---

Análise multi-agente (16 agentes, 2026-07-05) sobre o pedido de "área de sequência de tarefas tipo ETL" no [[orquestra-sge-app]]. Relatórios brutos em `/tmp/claude-0/-root/6be00b90-dd3a-4cf7-bd42-c91cbf215942/tasks/analise/` (efêmero).

**O que JÁ existe (não recriar!):** `FluxoEditor.tsx` (2.273 linhas, @xyflow/react 12) embutido na tela Etapas (`/jobs`, toggle Lista↔Fluxo) — canvas com paleta, dependências N:N, paralelismo real, decisão binária (4 condições: contagem/query/linhas_job/valor_sql), nós SQL/notificação Teams, simulação, ciclo no backend, publica DAG. Persiste em `GET/POST /pipelines/{p}/fluxo` sobre `etl_pipeline_job` (depends_on_jobs CSV + condition_json/notify_json/sql_json/layout_x-y, migrations 038/043/048/049/051). Factory compila grafo real (modo deps explícitas, BranchPythonOperator, trigger rules NONE_FAILED_MIN_ONE_SUCCESS/NONE_SKIPPED). Construído nos PRs #100–#139 (jun/2026). Decisões travadas em docs/DESIGN_condicao_branch.md, DESIGN_nova_experiencia.md (NÃO criar tabela de arestas; VIEW primeiro), DESIGN_navegacao_regroup.md (toggle, não tela — a revisar).

**Bugs ativos confirmados:** (1) colisão de execution_id=ts_nodash — PK de etl_job_execution SEM pipeline + índice único ux_etl_execution + coluna pipeline NULLable; Fase 2 da migration 027 nunca feita; 2 pipelines no mesmo tick com job homônimo = run derrubado com card Teams "Não identificado". (2) job_name aceita ESPAÇO (jobs.py:40) mas Airflow rejeita task_id com espaço → DAG publicada quebra no import. (3) nó SQL/valor_sql degradam p/ None em erro → decisão roteia ramo falso EM SILÊNCIO. (4) nó SQL citado em ramo de decisão gera task inexistente (_decision_block só trata notif). (5) /execucoes/rerun não casa execution_id (limpa run errado). (6) /factory/preview só conhece ondas (mente p/ grafos). (7) fila dashboard.py:146-151 agrega sem pipeline. (8) FluxoEditor sem gate viewer; multi-delete só apaga 1º; console.log temporário; SUM(duration) engana com paralelo em Logs/KPIs/SLA preditivo/daily report; SKIPPED invisível (5 consumidores de status, não 3 — inclui teams_end e daily report); órfãos não-DataStage sem reconciliação automática (ligar com a tela [[orquestra-finalizacao-manual]]).

**Executado:** ondas 0+1 no **PR #165** (mergeado na main em 2026-07-05, merge `b694257`): higiene do editor (readOnly/multi-delete/ciclo client-side), espaço em job novo bloqueado, fila dashboard e rerun corrigidos (rerun ganhou `dag_run_id` no clearTaskInstances — sem ele limpava TODOS os runs), fail-loud `on_error` em decisão/nó SQL (default carimbado no save; legado preservado até republicar), tela `/fluxos` (perm tela_jobs, sem migration) e tipo `http` de 1ª classe (sem default httpbin). Revisão adversarial: 13 achados corrigidos; 508 testes.

**Conexões (2026-07-05, EM PRODUÇÃO — baseline rodado, schema_version reconciliado, conexões nativas confirmadas funcionando pelo usuário):** #166 = fix sintaxe migration 025 (literais adjacentes) + `migrate.py --baseline` (prod tinha 37 "pendentes" por drift do etl_schema_version — schema aplicado por fora; rodar `--baseline` no servidor após deploy). #168 (reabertura do #167, fechado quando a branch-base sumiu) = componentes SQL do fluxo (nó SQL/decisão/storedproc + preview/simulação) resolvem conexão NATIVA via `conn_resolver.abrir_conexao_mssql` (etl_conexao primeiro, Airflow fallback — mesma regra da Cópia); conditions/job_operators são HOT (valem no próximo run), nó SQL exige republicar a DAG; requer ORQUESTRA_CONN_KEY nos workers. Gotcha aprendido: merge com --delete-branch FECHA PRs encadeados na branch apagada (GitHub não retargeteia) — retargetear antes de mergear.

**Onda 2 (2026-07-05, PR #169, merge `297a88a` — EM PRODUÇÃO desde 2026-07-06, migration 058 aplicada sem erros):** migration 058 (PK com pipeline — REESCRITA pela revisão: drop dinâmico de índices dependentes/Msg 5074, atomicidade XACT_ABORT, QUOTED_IDENTIFIER ON, sp_etl_job_execution_log v2 reinicia relógio no reprocesso; APLICAR EM JANELA sem execuções, sqlcmd só com -b -I), wall-clock em todos os consumidores (incl. SLA preditivo), SKIPPED de 1ª classe (t_flow_close só em pipelines com decisão; republicar DAGs para ativar), reconciliar genérico via Airflow REST. Pós-merge: deploy + migration em janela + republicar DAGs com decisão/nó SQL.

**Onda 3 (2026-07-06, PR #170 MERGEADO na main, merge `e906117` — EM PRODUÇÃO desde 2026-07-08, SMOKES PENDENTES; SEM migration; DAGs existentes byte-idênticas, republicar só quem usar switch/rename):** (a) **Decisão switch N-way** — condition_json ganha `casos` [{nome, operador, valor, ramo[]}] avaliados em ordem (primeiro que casa vence) + `ramo_senao` + on_error falhar|senao; SEM migration (coluna sem constraint); binária legada intocada — DAG **byte-idêntica** verificada empiricamente main×branch (import de eval_switch só com casos); runtime `eval_switch`/`_obter_valor` em conditions.py; UI com handle colorido por caso (paleta CASO_CORES), editor de casos com reordenação, conversão binário↔switch remapeando arestas (sim→caso_1, não→senão). (b) **Rename transacional de job** — POST /pipelines/jobs/{p}/{j}/rename: estratégia CÓPIA (FK de params sem ON UPDATE CASCADE; sys.columns cobre as 2 shapes de prod), reescreve CSV+condition_json dos irmãos (child_job preservado — é filho DataStage), lineage e histórico (etl_job_execution job_name+task_id; etl_ds_job_log); 409 com RUNNING; exige REPUBLICAR a DAG; UI renomeia nó salvo com modal. Revisão adversarial frontend: 10 achados, corrigidos (dirtyRef guard no rebuild — refetch não descarta edições; cache TanStack reescrito no rename; isNew limpo pós-save; id de aresta recomposto). 42 testes novos; suíte 568+5 baseline. Deploy: sem migration, deploy.sh normal (api+dist+dags).

**UX do painel de propriedades — CONCLUÍDO na main em 2026-07-07 (EM PRODUÇÃO desde 2026-07-08):** 5 PRs mergeados: #171 (quick wins: validação inline jobTypeFieldsErrors no painel+guard do save, Esc fecha, dblclick), #172 (extração p/ components/etapas/paineis/* + fluxoTypes.ts; FluxoEditor 3.072→1.790 linhas), #173 (DOCK INFERIOR 3 estados: colapsado 36px c/ resumo do nó / aberto arrastável 180-560px / max 70% = modo focado; localStorage orq.fluxo.dock.*; barra-guia sem seleção; dblclick abre maximizado), #174 (layouts largos: decisão 2 colunas c/ casos em TABELA + coluna Ramo→ + senão no tfoot, etapa 2 col c/ JobTypeFields sem compact, SQL editor rows=10 + preview ao lado, notificação 2 col, Excluir no topo direito; gotcha Tailwind: text-[11px] não vence text-sm base do Input — usar text-xs), #175 (handles 10→14px, espaçamento 20px/caso, rótulos flutuantes das saídas com nó selecionado — acessibilidade daltônicos), #176 (PALETA vira coluna FIXA à esquerda recolhível a trilho de 44px, persistida em orq.fluxo.paleta.estado — saiu do overlay que cobria o canvas). EXTRAS do benchmark (2026-07-07, todos na main): #177 pendências visíveis (anel/ponto âmbar no nó + chip no header do dock — mesma régua dos guards do save), #178 micro-interações (Enter abre/max, ←/→ navegam SÓ no modo focado — fora dele setas são nudge do RF, Ctrl+Enter salva, lápis rename no header, ⤢ ampliar nos editores SQL, hover no caso destaca aresta), #179 dock sem seleção mostra propriedades do PIPELINE (GET /pipelines?filter_name= match exato; queryKey ['fluxos-biblioteca'] compartilhada; contagem de nós por tipo), #180 ajuda contextual (ui/Hint.tsx + prop hint em Input/Select/Textarea; textos derivados das validações reais) + rebuild dist ea82036. GOTCHA agentes paralelos: working tree compartilhada — 2 agentes em branches diferentes se atropelam (um trocou o branch do outro; refs corrigidas à mão); paralelizar só com worktree isolado. Deploy: SÓ ui-react/dist (deploy.sh parte automática, sem confirmações). Original (para histórico): substituir o aside fixo de 320px por **DOCK INFERIOR com 3 estados** (colapsado 36px c/ resumo do nó / split arrastável / maximizado = modo focado), padrão Informatica IDMC/ADF — benchmark visual confirmou que NENHUMA ferramenta de mercado usa aside fixo; screenshots salvos em scratchpad/bench/ (efêmero). Entrega em **5 PRs**: 1) quick wins (validação inline c/ jobTypeFieldsErrors, Esc fecha, dblclick); 2) extração dos painéis p/ components/etapas/paineis/*; 3) mecânica do dock + persistência localStorage; 4) layouts largos (casos do switch em TABELA c/ coluna Ramo→, JobTypeFields sem compact, SQL editor largo + preview ao lado); 5) handles switch maiores + rótulo via NodeToolbar. Achados-chave do UX report: painel monta/desmonta por seleção (reflow), fonte 10px, campos ~100px, PainelVazio é código morto (reusar no dock — painel nunca vazio), validação só no toast do save. Smokes da onda 3 (switch/rename em prod) ADIADOS até concluir isso; onda 4 vem depois.

**BACKLOG PRIORIZADO (atualizado 2026-07-08, alinhado com o usuário):**
0. ~~Deploy acumulado (#170–#184)~~ **FEITO — em produção desde 2026-07-08** (migration 059 aplicada, api + dags + dist). Usuário confirmou; smokes ainda NÃO rodados.
1. **SMOKES pós-deploy (próximo passo, checklist):**
   (a) **Switch N-way** — criar decisão com 2+ casos + senão, reordenar casos, converter binário↔switch (conferir remapeamento sim→caso_1/não→senão), salvar, publicar e rodar; conferir roteamento e on_error falhar|senão.
   (b) **Rename transacional** — renomear job COM histórico de execução; conferir CSV depends_on/condition_json dos irmãos, lineage, 409 com RUNNING; republicar a DAG após rename.
   (c) **Republicar DAGs** de pipelines com job shell/python — fix #181 (repr) e modos do #183 só valem ao republicar; #182 (template_ext) já vale no próximo parse sem republicar.
   (d) **Nó Python v2** — testar os 3 modos: arquivo (SSH script_path), código embutido (SFTP publica + sobrescreve + roda), módulo legado (DAG byte-idêntica); job python legado com comando fora da regex de módulo agora mostra pendência e bloqueia save (esperado).
   (e) **UI nova** — dock 3 estados + persistência localStorage, paleta fixa recolhível, pendências (anel âmbar + chip), teclado (Enter/Ctrl+Enter/setas no modo focado), dock sem seleção (propriedades do pipeline), hints (?), bancos por conexão no storedproc/decisão (trocar conexão zera banco).
   (f) **Nó Shell** — job com comando path puro/palavra única e comando terminando em .sh (casos dos bugs #181/#182) rodando ok.
2. **Melhorar tipos de nó EXISTENTES da paleta** (nota do usuário 2026-07-07) — shell e python já atacados (#181–#183); levantar dores restantes com ele: candidatos = nó SQL, notificação, storedproc, http.
3. **Onda 4 em 2 blocos**: BLOCO 1 = `espera_arquivo` (sensor Airflow modo reschedule: caminho/máscara, timeout, intervalo) + `subpipeline` (TriggerDagRun com BYPASS da agenda do filho) + botão **Publicar/Executar na toolbar** do editor (hoje publish só no modal pós-save; Executar = trigger via Airflow REST — o Save/Compile/Run do DataStage). BLOCO 2 = `copia_dados` como etapa do fluxo (reusa módulo Cópias, guarda 409 anti-duplicidade — o de MAIOR valor) + `script_sql` (DML controlado como etapa; nó SQL atual só SELECT escalar).
4. **Onda 5 depois**: preview honesto p/ grafos (/factory/preview ainda pensa em ondas), overlay de execução AO VIVO no canvas (verde/vermelho/rodando por nó — requer persistir run_id; o item que ex-usuários DataStage mais sentem falta), import de Sequence DSX preservando grafo (hoje lineariza).

**(2026-07-08): usuário chegou com PRIORIDADE NOVA fora deste backlog — tratar primeiro; smokes acima ficam na fila.**

**Benchmark — itens AINDA NÃO usados (guardar; quase todos se encaixam DENTRO da onda 4):**
- **Abas com responsabilidade fixa + badge de erro por aba** (IBM/NiFi/ADF) — adiadas de propósito; o nó `copia_dados` (conexões/tabelas/transformações/partição) é o gatilho para estreá-las.
- **Dados reais ao lado do formulário** (n8n NDV) — preview de colunas/amostra da origem no `copia_dados`; testar máscara contra o diretório na `espera_arquivo`.
- **Tearsheet "Aplicar e voltar"** (IBM) — mapeamento de colunas/transformações da cópia em tela cheia com retorno garantido ao painel.
- **"Executar só este nó"** (n8n Execute node) — ambicioso, exige backend; degrau acima do Executar-pipeline da toolbar.
- **Expression Builder dedicado** (ADF) — só se expressões ganharem complexidade.
- DESCARTADO com registro: Save/Cancel transacional por painel (IBM/NiFi) — conflita com o modelo de edição ao vivo + "Salvar fluxo"; retrabalho de estado grande.

**NOTA do usuário (2026-07-07): antes/junto dos itens novos, "precisamos fazer funcionar melhor algumas das paletas existentes"** — ou seja, TIPOS DE NÓ já existentes na paleta precisam de melhorias de funcionamento (detalhar com ele quais: candidatos prováveis = nó SQL, notificação, storedproc, http). Levantar as dores específicas antes de iniciar a onda 4.

**Bancos POR CONEXÃO em 100% do Orquestra (2026-07-07, PR #184 na main — EM PRODUÇÃO desde 2026-07-08):** o banco-alvo do storedproc listava bancos de servidor FIXO (query global /jobs/databases sem parâmetro). Regra unificada: toda lista de bancos segue a CONEXÃO selecionada via /jobs/databases?conn_id= (credencial nativa; host fallback) — JobTypeFields busca sozinho (Fluxo+Lista; trocar conexão ZERA o banco; sem conexão select desabilitado), decisão contagem/query ganhou BancoCondicao (select por conexão; campo livre só na 'Conexão padrão'), nó SQL já seguia a regra. Plumbing dbServer/dbDatabases removido dos pais.

**Nó PYTHON v2 (2026-07-07, PR #183 MERGEADO na main, merge 4a8ca4c — EM PRODUÇÃO desde 2026-07-08, migration 059 aplicada; smokes dos modos novos pendentes):** um nó só com seletor "Modo de execução" (decisão UX validada por agente): 'arquivo'=Script no servidor (default nó novo; SSH + script_path absoluto → <interp> <path>), 'codigo'=Código embutido (SSH + destino_dir + arquivo .py + textarea; PythonScriptOperator publica via SFTP c/ mkdir -p fail-loud, SOBRESCREVE a cada execução, depois roda cd dir && interp arquivo), 'modulo'=legado worker (python_json NULL; DAG byte-idêntica). python_json (migration 059) segue o padrão sql_json (validate/normalize/persist/round-trip; chave 'python' SEMPRE no payload de nó python — null volta ao legado; frontend antigo sem a chave não zera). PythonScriptOperator: template_ext=() + shlex.quote em RUNTIME. Sublabel no canvas: script @ srv / código @ srv / módulo (worker). 28 testes backend (test_dag_factory_python, test_python_script_operator, test_jobs_python_node). ATENÇÃO: job python legado com job_command fora da regex de módulo agora mostra pendência e bloqueia save do fluxo (espelha o runtime). Jobs novos: modos novos valem ao REPUBLICAR a DAG.

**Nó SHELL — verificado e corrigido (2026-07-07, PR #181 na main; EM PRODUÇÃO desde 2026-07-08):** a execução JÁ era via SSH no servidor escolhido (ShellOperator herda SSHOperator; ssh_conn_id do job flui UI → register/fluxo gravam → sp_etl_pipelines_pendentes_criar devolve → factory emite → executa no servidor; fallback SSH_CONN_ID do pipeline, default ssh_lnxprd021). BUG achado na EMISSÃO: shlex.quote não põe aspas em comando sem caractere especial — path puro (/opt/scripts/run.sh, o placeholder da UI!) ou palavra única (ls) geravam DAG inválida no import (SyntaxError/NameError); só comando com espaço funcionava. Fix: repr() no _task_block (comando byte a byte; com espaço o literal fica IDÊNTICO ao anterior). 9 testes em tests/test_dag_factory_shell.py (extraem kwargs do código gerado via AST). Republicar DAGs de pipelines com job shell após o deploy. **BUG 2 (incidente real SHELL_LIMPA_FS_DATASTAGE, PR #182):** SSHOperator herda template_ext=('.sh',) → comando TERMINANDO em .sh (ex.: `cd /x && ./script.sh`) era interpretado pelo Jinja como CAMINHO de arquivo de template → TemplateNotFound antes do SSH. Fix: ShellOperator com template_ext=() em dags/utils/job_operators.py (macros {{ ds }} preservadas); vale p/ DAGs JÁ publicadas no próximo parse — SEM republicar (operador importado em runtime). Screenshot da config do usuário em /root/shell_config.png.

**Roadmap recomendado (ondas):** 0-Correções (higiene editor, espaço no nome, fila, rerun, on_error fail-loud carimbado pela API no save — não pela factory); 1-Tela própria `/fluxos` (wrapper fino do MESMO FluxoEditor, lista de fluxos + tela cheia) + tipo `http` (factory já suporta; remover default httpbin.org); 2-Fundação telemetria (migration Fase 2 da 027 com backfill/NOT NULL/drop ux + wall-clock em TODOS os consumidores + SKIPPED só p/ state='skipped' via get_task_instances + reconciliação genérica de órfãos); 3-Decisão v2 (switch N-way — raio maior que parece: _validate_condition, fecho transitivo, ciclo em 2 caminhos, GET /fluxo, simulador; rename transacional incl. source_job/child_job/params sem ON UPDATE CASCADE); 4-Tipos novos (espera_arquivo sensor reschedule, subpipeline TriggerDagRun c/ bypass de agenda do filho, copia_dados reusando módulo Cópias c/ guarda 409, script_sql); 5-Preview honesto + overlay de execução no canvas (precisa persistir run_id) + import de Sequence DSX preservando grafo (converter etl_ds_seq_flow.flow_json — hoje o import lineariza).
