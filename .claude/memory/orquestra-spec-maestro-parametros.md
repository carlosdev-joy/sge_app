---
name: orquestra-spec-maestro-parametros
description: Spec do Maestro — avatar de chat que ajuda a preencher os parâmetros DataStage em Etapas/Fluxos; rascunho 2026-09-10 aguardando aprovação do usuário
metadata: 
  node_type: memory
  type: project
  originSessionId: 55c7dc0b-089c-4e39-9b3c-c60297f1190c
  modified: 2026-09-10T09:54:36.888Z
---

**Spec:** `docs/spec-maestro-parametros.md` no repo `carlosdev-joy/sge_app` (`/opt/orquestra-dev`). **Status: APROVADA 2026-09-10 ("Pode desenvolver um avatar que faça sentido com o contexto da aplicação, os demais pode seguir"); 🔧 F1 EM ANDAMENTO na branch `feat/maestro-f1`** (o usuário pediu em 2026-09-10: "avatar de chat conversação que apoia na criação na tela de etapas / fluxos, nome Maestro; a pessoa explica o cenário e ele ajuda a preencher cada campo do parâmetro; se o cenário não existe, informar a procurar o administrador"). Decisões fechadas em §8: avatar SVG próprio derivado do `Logo.tsx` (regente conduzindo as 3 linhas do pipeline, F2), botão à direita de "Importar do DataStage", permissão `tela_jobs`, retenção 180 dias.

**✅ F1 = PR #382 MERGEADA (`2062889`); ✅ F2 = PR #383 MERGEADA (`bf4a1a9`); ✅ F3 = PR #384 MERGEADA (`9e0d5d2`); ✅ F4 (última) = PR #385 MERGEADA (`8758b7e`, autorizado em 2026-09-10). 🏁 SPEC CONCLUÍDA NO CÓDIGO (F1–F4 = PRs #382–#385). ⏳ DEPLOY EM PRODUÇÃO PENDENTE (pelo usuário): roteiro em `docs/release-notes/maestro.md` — depende de as migrations 107–109 (parâmetros DataStage) já estarem lá; migration 110 na 6c (s), `api/` sim, `dags/` sim SEM restart do worker, `dist/` sim, `config/` n; depois: chave do provedor em Admin › Caixa Seguro IA (+ Verificar) e ligar em Admin › Maestro. Smoke pós-deploy a–m na release note.** F4 entregou: manual §3.11/§4.9/§4.6/§3.10/FAQ, `docs/release-notes/maestro.md` (roteiro de deploy: 110 na 6c, `api/`, `dags/` sem restart do worker, `dist/`, `config/` n; depois: chave em Caixa Seguro IA e ligar em Admin › Maestro), linha em Funcionalidades, spec concluída com §7 resultado + reconciliação §3. Smoke §7 no DEV: 10 cenários da semente pela conversa real (provedor de mentira v2 `fake_provider_v2.py` no container lendo o catálogo do prompt), 3 pedidos → Admin, 0 segredo. ⏳ **Pendentes de ambiente real (registrados na release note):** b, d, i (browser), h (chave inválida real), j, k (gateway da Caixa/Anthropic com histórico), m (DAG agendada). Backlog: sugestões de abertura = só os 4 primeiros cenários ativos (escolher por flag/ordem do admin); erro do provedor (502) chega ao chat como o detail do provedor. F3 entregou: rotas `/maestro/admin/*` (config, cenarios CRUD + validar, pedidos tratar) com `get_admin_user`; `dags/etl_log_cleanup.py` task `limpar_conversas_maestro` (180 dias, pymssql `%s`); aba Admin › Acessos & Comunicação › Maestro (`components/admin/MaestroTab.tsx`, `MaestroCenarioModal.tsx`, `lib/maestroAdmin.ts`). GOTCHAs F3: `INSERT …; SELECT SCOPE_IDENTITY()` num único execute do pyodbc dá "No results" (usar `OUTPUT INSERTED.id`, convenção do repo — pego no smoke do DEV); comparação do eslint deve usar só a 1ª linha da mensagem (o texto traz `arquivo:linha` e uma inserção acima desloca ocorrências pré-existentes); `git stash push -u -- <paths>` para o baseline; DEV: `docker exec orquestra-dev-airflow-worker-1` com `MsSqlHook('SQL14_DMDB41')` funciona para exercitar funções de DAG; smoke scripts em scratchpad `smoke_maestro_f3.py`/`pedido_smoke.py`. **Próximo: F4** (manual §3.11 + nota §3.10/§4/FAQ, `docs/release-notes/maestro.md`, `ORQUESTRA_Funcionalidades_e_Beneficios.md`, smoke §7 no DEV com o provedor de mentira dentro do container, status da spec → concluída). F2 entregou: `lib/maestro.ts` (puro), `lib/maestroStatus.ts`, `components/etapas/MaestroAvatar.tsx` (SVG regente + 3 linhas do logo), `MaestroPainel.tsx` (apresentação), `MaestroChat.tsx` (portal `document.body` + `data-modal-exempt`, Escape com stopPropagation), botão no `JobTypeFields` à direita de "Importar do DataStage", bancada seção 7 + `tests/test_maestro_front.py`. GOTCHAs F2: bancada precisa de shim `react-dom.createPortal` inline e `global.document` mínimo; eslint baseline via `git stash push -u -- ui-react/src tests/js ...` (84 = 84); container do DEV NÃO alcança o host (172.19.0.1 bloqueado) → provedor de mentira roda DENTRO do container (`docker exec ... nohup python /tmp/fake_provider.py 18081`, base_url `http://127.0.0.1:18081`), scripts em scratchpad `fake_provider.py`/`config_ia_dev.py`/`smoke_maestro_f2.py`; sem Chrome nesta VPS → conferência visual (tema escuro, z-index, dock) só em homolog. Achados aplicados da revisão F2: Encrypted sobre token gravado preserva `tem_valor`; cartão usa `r.referencia`; pedido vigente por ref; Ctrl+Enter não envia; ids sequenciais. F1 entregou: migration `110_maestro.sql` (16 lotes, 10 cenários semeados, `maestro_enabled=0`), `services/caixa_ia.chat_conversa` + `normalizar_mensagens`/`transcrever` (chat() dos assistentes do Caixa delega, corpo idêntico), `services/maestro.py`, `routers/maestro.py` (`GET /maestro/status`, `POST /maestro/conversar`, `GET /maestro/historico`, gate `tela_jobs`), testes `test_maestro_servico/rota`, `test_caixa_ia_conversa`. ✅ DEV desta VPS: 110 aplicada e reexecutada (0 erros), API reconstruída, smoke sem provedor (interruptor 0/1 sem chave → enabled=false, 503 distintos, 422, 401). ⚠️ DEV NÃO tem provedor de IA (`caixa_ia_*` vazio): conversa real com o modelo só na F2/F4 — se o usuário fornecer uma chave Anthropic no DEV (Admin › Caixa Seguro IA) dá para smokar. GOTCHAs F1: `docker cp sql orquestra-api:/tmp/dev_sql` cai DENTRO da pasta antiga (copiar o arquivo direto para `/tmp/dev_sql/migrations/`); `up -d` recria o container e apaga scripts em `/tmp` de lá; `etl_schema_version` não tem coluna `version_id`. Achados da revisão aplicados: teto 4.000 só na mensagem do usuário (assistant truncado em 6.000), regex do bloco JSON sem atravessar fences, histórico escolhe conversas antes das linhas.

**Próximo passo:** após o merge da F1, abrir F2 (`feat/maestro-f2`): `lib/maestro.ts` (puro: contextoDoEditor sem Encrypted, propostaParaEditor, aplicarProposta por nome), `components/etapas/MaestroChat.tsx` (portal `z-[60]`, tokens nativos, avatar SVG próprio = regente conduzindo as 3 linhas do `Logo.tsx` em ciano/azul, estados ouvindo/pensando), botão no `JobTypeFields.tsx:664` à direita de "Importar do DataStage" (só datastage + `useMaestroAtivo`), `paramsRef` no aplicar, sugestões de `GET /maestro/status`, histórico; bancada `tests/js/ds_params_harness.cjs`; `dist/` por último. Contrato da API já fechado nesta F1 (ver `routers/maestro.py`).

**Desenho (4 fases):** F1 migration `110_maestro.sql` (`etl_maestro_cenario` catálogo semeado + `etl_maestro_conversa` log + chave `maestro_enabled` em `etl_app_config`), `services/maestro.py` (system prompt GERADO do vocabulário de `job_params`, proposta em bloco JSON validada por `normalizar_lista`/`resolver_preview`), `caixa_ia.chat_conversa` multi-rodada, `routers/maestro.py` atrás de `tela_jobs`; F2 `MaestroChat.tsx` (portal `z-[60]`) + `lib/maestro.ts` no `JobTypeFields` (botão à direita de "Importar do DataStage"), "Aplicar no editor"; F3 Admin aba Maestro (interruptor, catálogo, pedidos não atendidos); F4 manual §3.11, release note, smoke.

**Decisões-chave:** reusa o provedor de IA do Caixa (`caixa_ia_*`, incl. gateway da Caixa — texto puro, sem tool-use) com interruptor próprio; "cenário não existe" = fora do catálogo OU proposta que não passa na régua do servidor → "procure o administrador" + registro `nao_atendido` visível no Admin; Maestro nunca salva (só aplica no editor) e nunca vê/grava valor Encrypted.

**Why:** o vocabulário (origem/meses/âncora/dias/formato) é a barreira de adoção da spec [[orquestra-spec-parametros-job-datastage]]; o catálogo dá ao admin controle do que o Maestro promete e mostra a demanda reprimida.

**How to apply:** não implementar antes da aprovação; ao aprovar, abrir F1 pelo fluxo de sempre (branch a partir de `origin/main`, revisão adversarial, PR com 3 blocos, merge só com autorização). Pendências em aberto na spec §8 (posição do botão, avatar, permissão, retenção).
